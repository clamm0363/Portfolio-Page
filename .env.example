# Cloudflare Tunnel Token
# Create at: Cloudflare Zero Trust → Networks → Tunnels → Create a tunnel
# Copy the token from the tunnel configuration
TUNNEL_TOKEN=your-cloudflare-tunnel-token-here

# PostgreSQL Database Configuration
POSTGRES_DB=umami
POSTGRES_USER=umami
POSTGRES_PASSWORD=your-secure-password-here

# Umami Hash Salt (generate a random string with: openssl rand -hex 32)
UMAMI_HASH_SALT=your-random-64-character-hex-string-here

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Create a Cloudflare Tunnel and copy the token to TUNNEL_TOKEN
# 3. Change POSTGRES_PASSWORD to a strong password
# 4. Generate UMAMI_HASH_SALT with: openssl rand -hex 32
# 5. Never commit .env to git
# 6. Export environment variables before deploying: export $(cat .env | grep -v '^#' | xargs)
