# Cloudflare Tunnel Token
# Create at: Cloudflare Zero Trust → Networks → Tunnels → Create a tunnel
# Copy the token from the tunnel configuration
TUNNEL_TOKEN=your-cloudflare-tunnel-token-here

# PostgreSQL Database Configuration
POSTGRES_DB=umami
POSTGRES_USER=umami
POSTGRES_PASSWORD=your-secure-password-here

# Umami Hash Salt (generate a random string with: openssl rand -hex 32)
UMAMI_HASH_SALT=your-random-64-character-hex-string-here

# Portainer API Token (for automated deployment from Cursor)
# Create at: Portainer → My account → API keys
# Description: "Cursor Deployment"
PORTAINER_API_TOKEN=ptr_your_portainer_token_here

# Portainer URL (optional, defaults to http://192.168.0.95:9000)
PORTAINER_URL=http://192.168.0.95:9000

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Create a Cloudflare Tunnel and copy the token to TUNNEL_TOKEN
# 3. Create Portainer API token and add it to PORTAINER_API_TOKEN
# 4. Change POSTGRES_PASSWORD to a strong password
# 5. Generate UMAMI_HASH_SALT with: openssl rand -hex 32
# 6. Never commit .env to git
