# Umami Database Configuration
UMAMI_DB_NAME=umami
UMAMI_DB_USER=umami
UMAMI_DB_PASSWORD=change-this-password

# Umami Hash Salt (generate a random string)
UMAMI_HASH_SALT=change-me-to-random-string

# Portainer API Token
# Create at: Portainer → User Settings → Access tokens
PORTAINER_API_TOKEN=ptr_your_portainer_token_here

# GitHub Personal Access Token (for private repository deployment)
# Create at: https://github.com/settings/tokens
# Required scope: repo (Full control of private repositories)
GITHUB_TOKEN=ghp_your_github_token_here

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Change the passwords and hash salt to secure values
# 3. Create Portainer API token and add it above
# 4. Create GitHub Personal Access Token (if repo is private) and add it above
# 5. Never commit .env to git
